/**
 * 工作流编辑视图控制器对象
 */
var WFEditViewControllerBase = EditViewControllerBase.extend({
	construct: function(config) {
		arguments.callee.$.construct.call(this,config);
	},
	init:function(params){
		arguments.callee.$.init.call(this,params);
		var me = this;
	}
});













